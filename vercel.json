{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then prisma migrate deploy && (node prisma/seed-demo.mjs || true) && next build; else next build; fi",
  "crons": [
    {
      "path": "/api/cron/alerts",
      "schedule": "0 6 * * *"
    }
  ]
}
